<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
  <url><loc>{{ request.host_url }}</loc><changefreq>daily</changefreq><priority>1.0</priority></url>
  <url><loc>{{ request.host_url }}evenements</loc><changefreq>daily</changefreq><priority>0.9</priority></url>
  <url><loc>{{ request.host_url }}bourses</loc><changefreq>daily</changefreq><priority>0.8</priority></url>
  {% for event in events %}
  <url>
    <loc>{{ request.host_url }}evenement/{{ event.slug }}</loc>
    <lastmod>{{ event.date_collecte.strftime('%Y-%m-%d') }}</lastmod>
    <changefreq>weekly</changefreq>
    <priority>0.7</priority>
  </url>
  {% endfor %}
</urlset>